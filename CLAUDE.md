# CLAUDE.md — WE'RE NOT WOLF

เอกสารสเปค/บริบทถาวรของโปรเจกต์ อ่านไฟล์นี้ก่อนเริ่มงานทุกครั้ง โดยเฉพาะหัวข้อ 3 ระบบใหม่
ด้านล่าง (โหมดเกม / เล่าเรื่องหลังจบ / การ์ดโชคดี-โชคร้าย)

> หมายเหตุประวัติ: ไฟล์นี้ถูกสร้างขึ้นใหม่ (ของเดิมหาย/ไม่เคยถูก commit) จากสเปคที่เจ้าของ
> โปรเจกต์ให้มา + การอ่านโค้ดจริง ส่วนที่ทำเครื่องหมาย **(ข้อเสนอ — รอ approve)** คือสิ่งที่
> Claude เสนอใหม่ ไม่ใช่การตัดสินใจเดิมของเจ้าของโปรเจกต์ ต้องยืนยันก่อน implement

---

## กฎการทำงานสำคัญที่สุด

ก่อน implement แต่ละระบบ (1, 2, 3) ให้ **ถามยืนยันทิศทางก่อนทุกครั้ง** ไม่เดาเองแล้วลงมือ
โดยเฉพาะจุดที่ทำเครื่องหมาย "ยังไม่ตัดสินใจ" หรือจุดที่ implement ได้มากกว่า 1 แบบ (เก็บ state
ตรงไหน / ออกแบบ schema ยังไง) — สรุปแผนสั้น ๆ รอ approve ทีละระบบ ห้ามรวบทำ 3 ระบบรวดเดียว

ทำเรียงทีละระบบ 1 → 2 → 3

---

## สถาปัตยกรรม (สรุปจากโค้ดจริง)

- **client/** — React 18 + React Router v6 + Vite, plain CSS (design tokens ใน `client/src/styles/global.css`)
- **server/** — Node/Express + MySQL (mysql2) + Socket.io ; state เกมอยู่ใน memory (`gameStore.js`) DB ใช้เก็บ room/player/message/user
- **shared/** — โค้ดที่ client/server ใช้ร่วม (เช่น `leveling.js`)

### game state machine (server)
- Phases: `lobby → night_zero → day → voting → results → night → (loop) → ended` — `server/src/game/constants.js`
- First-night flow: แจก role → `night_zero` รอทุกคนกด "ดูแล้ว" (`player:ready`) → `day` รอบที่ 1 โดยไม่ Resolve Night Action
- Loop ปกติ: `RESULTS → NIGHT → DAY` (`NEXT_PHASE` ใน `phaseManager.js`) ; round +1 ตอนเข้า night
- เวลาแต่ละ phase: `getPhaseDurationMs(roomId, phase)` อ่านจาก `room.phaseDurations` (host ตั้ง) — `results` คงที่ 10s
- ตัวจับเวลา/advance รวมที่ `advancePhase()` (กันซ้อนด้วย `advancingRooms` set) เรียกได้ 3 ทาง: timer หมด, โหวต/ready ครบ, host skip

### 6 roles เดิม (`constants.js`)
`villager, werewolf, seer, bodyguard, silencer, fool`
- faction (`ROLE_FACTION`): village = villager/seer/bodyguard/silencer ; werewolf = werewolf ; neutral = fool
- night action roles: werewolf, seer, bodyguard, silencer
- `PLAYER_LIMITS` = { MIN: 4, MAX: 8 }
- role composition: host ตั้งเองผ่าน `RoomConfigPanel` ใน Lobby (ส่ง socket `room:config`) → เก็บใน `room.roleConfig` ; default จาก `ROLE_DISTRIBUTION`
- แจก role ตอน `game:start` → `distributeRoles(players, roleConfig)` (`Roledistributor.js`) validate ด้วย `validateConfigForPlayerCount`

### จุดสำคัญเรื่องข้อมูล (สำหรับระบบ 2)
- **vote**: `voteManager.js` เก็บใน memory (`voteStore`) และ **ถูกล้างทุกรอบ** (`clearVoting`) — ไม่มีประวัติสะสมข้ามวัน
- **night action**: `room.nightActions` reset ทุกคืน (`initNightActions`) — `resolveNightActions()` คืน `prevented`/`protectedIds` แต่ไม่เก็บสะสม
- **chat**: เก็บลง DB ตาราง `messages` (มี `player_id`, `channel`, `sent_at`) — ข้อมูลนี้ query ย้อนได้จริง
- **death**: `players.is_alive` ใน DB (สถานะสุดท้าย) แต่ **ไม่เก็บลำดับ/สาเหตุการตาย**
- สรุป: chat สะสมได้จาก DB ; แต่ vote/night/ลำดับการตาย ต้องเพิ่ม accumulator เบา ๆ ใน room state ระหว่างเกม (ดูคำถามก่อนเริ่มระบบ 2)

### win conditions (`winConditions.js`, `phaseManager.js`)
- village ชนะเมื่อหมาป่าตายหมด ; werewolf ชนะเมื่อ wolves ≥ villagers ; fool ชนะทันทีเมื่อ**ถูกโหวตเนรเทศ** (ไม่ใช่ถูกฆ่ากลางคืน)

---

## ระบบ 1: 2 โหมดเกม — Classic / โกลาหล (Chaos)  ✅ **IMPLEMENTED**

> สถานะ: ทำเสร็จแล้ว — เก็บ `gameMode` เป็น **column `game_mode` ENUM('classic','chaos')** ในตาราง `rooms`
> (ตัวเลือก A) + field บน in-memory room + `serializeRoom`. โกลาหลสุ่ม role ตอน `game:start` ด้วย
> `buildChaosRoleConfig()` (หมาป่า ≥1 และ ≤ ⌊players/4⌋) + เวลา fix `CHAOS_PHASE_DURATIONS` (25/90/25 floor 15).
> UI: การ์ดเลือกโหมดในหน้าสร้างห้อง + Lobby ซ่อนหน้าตั้งค่า role เมื่อเป็นโกลาหล. มีเทสต์ `roomConfig.test.js`.

### Classic
- host เลือก role composition เอง (ระบบตั้งค่าห้องเดิมใน Lobby)
- เวลาแต่ละ phase host ตั้งเอง (ระบบเดิม)
- โหวตกลางวันรอบเดียวจบ คะแนนสูงสุดออก เสมอ = ไม่มีใครออก (logic เดิมมีแล้ว)
- role เลือกได้: 6 ตัวเดิมเท่านั้น

### โกลาหล
- role **สุ่มอัตโนมัติทุกเกม** host ไม่ได้เลือก — คำนวณตอนกด "เริ่มเกม" จริง (validate กับจำนวนผู้เล่นจริง ณ วินาทีนั้น) ไม่ใช่ตอนสร้างห้อง
- validation บังคับ: **หมาป่าไม่เกิน 1/4 ของผู้เล่นทั้งหมด** และ **ต้องมีหมาป่าอย่างน้อย 1**
- เวลาแต่ละ phase fix: **กลางคืน 25s, แชทกลางวัน 90s, โหวต 25s** — ค่าคงที่เหล่านี้ห้ามต่ำกว่า **15s** ในทุกกรณี (เผื่ออนาคตปรับได้ → เก็บเป็น constant มี floor 15)
- โหวตรอบเดียวจบเหมือน Classic (ไม่มี trial 2 รอบ — ตัดออกแล้ว)

### UI
- เพิ่มตัวเลือกโหมดในหน้า**สร้างห้อง** (ก่อนเข้า Lobby)
- Classic → เข้า Lobby ตั้งค่า role/เวลาได้ตามเดิม
- โกลาหล → ใน Lobby **ซ่อน/ปิดหน้าตั้งค่า role** (เพราะสุ่มให้อัตโนมัติ + เวลา fix)

### จุดต้องถามก่อนเริ่ม (ระบบ 1)
- **เก็บ `gameMode` ไว้ตรงไหน** — เสนอ 3 ทาง:
  - (A) column `game_mode` ใหม่ในตาราง `rooms` + field บน in-memory room + `serializeRoom` (แนวเดียวกับ `status`/`is_private`) ← เสนอเป็นค่าเริ่มต้น
  - (B) ฝังใน `config` JSON เดิม (`config.gameMode`) ผ่าน `normalizeRoomConfig`
  - (C) in-memory เท่านั้น (ไม่ลง DB) — เพราะ Lobby/`game:start` อ่านจาก in-memory อยู่แล้ว

---

## ระบบ 2: เล่าเรื่องหลังเกมจบ (Post-game Highlights)

หน้าสรุปผล **ก่อน**โชว์ role ทุกคนแบบละเอียด แสดง **2–4 highlight** ที่มีข้อมูลรองรับจริงในเกมนั้น
(ข้ามอันที่ข้อมูลไม่พอ ไม่บังคับครบ) — ดึงจากข้อมูลที่ระบบมี/สะสมได้ ไม่เพิ่มการเก็บที่เกินจำเป็น

### รายการ highlight
| key | เงื่อนไข |
|-----|----------|
| ผู้ต้องสงสัยตัวยง | โดนโหวต/เสนอชื่อเยอะสุด แต่รอดจนจบ |
| หมาป่าจอมกล | เป็นหมาป่าที่ไม่เคยโดนโหวตแม้แต่ครั้งเดียวทั้งเกม |
| โหวตพลิกเกม | วันที่ผลโหวตกระทบผลแพ้ชนะโดยตรง (เช่นโหวตหมาป่าตัวสุดท้ายออก) |
| ผู้พลีชีพคนแรก | คนแรกที่ตาย (หมาป่าหรือโหวต) |
| นักพูดแห่งราตรี | พิมพ์แชทเยอะสุดในเกม |
| ผู้คุ้มกันเงียบ | Guardian ป้องกันสำเร็จ ≥ 1 ครั้ง (บล็อกการฆ่าได้จริง) |
| ความบังเอิญที่ไม่มีใครตั้งใจ | Fool ถูกโหวตออกจนชนะ |

### Style
ยืม signature จาก NewsPage (carved wood, หมุดไม้เก่า, sketch-border) ทำเป็น "ฉบับพิเศษ"
ก่อนเข้าหน้าสรุป role เต็ม

### ตัวอย่างประโยคเสียดสี **(ข้อเสนอ — รอ approve ตอนถึงระบบ 2)**
- ผู้ต้องสงสัยตัวยง: "โดนชี้หน้ากล่าวหาทั้งเกม แต่ยังยืนหยัดจนไฟดับ — บริสุทธิ์ หรือแค่โชคดี?"
- หมาป่าจอมกล: "เป็นหมาป่าแท้ ๆ แต่ไม่มีใครเอ่ยชื่อสักครั้ง เนียนจนน่าขนลุก"
- นักพูดแห่งราตรี: "พิมพ์ไปทั้งหมด {n} ข้อความ ปากคือพลัง (หรือหลุมพราง)"
- ผู้พลีชีพคนแรก: "คนแรกที่จากไป ขอให้เป็นความตายที่มีความหมาย"
> ยังต้องเขียนเพิ่มให้ครบทุก highlight ตอนถึงระบบ 2

### จุดต้องถามก่อนเริ่ม (ระบบ 2)
- field ไหนใน vote history ที่ใช้ได้จริงสำหรับนับ "โดนโหวตเยอะสุด" — **ผลตรวจโค้ด**: `voteManager` เก็บ `voteStore` per-round แล้วล้างทุกรอบ ไม่มีประวัติสะสม → ต้องเพิ่ม accumulator ระหว่างเกม (จะยืนยันวิธีตอนถึงระบบ 2)

---

## ระบบ 3: การ์ดโชคดี/โชคร้าย (Fortune Cards) — **20 ใบ**

### การสุ่ม 2 ชั้น (ทุกเช้า ต่อผู้เล่นแต่ละคน)
- **ชั้น 1 — ได้การ์ดไหม**: Classic = 40–45% · โกลาหล = 100% (การันตีได้ทุกเช้า)
- **ชั้น 2 — โชคดี/ร้าย (ถ้าได้การ์ด)**:
  - Classic: โชคดี 57% / โชคร้าย 43%
  - โกลาหล: โชคดี 40% / โชคร้าย 60%

### การถือ/การใช้
- **โชคดี**: เก็บ inventory เลือกใช้เอง — ถือได้สูงสุด **1 ใบ** (ได้ใบใหม่ระหว่างถือใบเก่า → popup ให้เลือกว่าจะทิ้งใบไหน)
- **โชคร้าย**: มีผลทันทีที่สุ่มได้ ไม่มีทางเลือก

### กฎเหล็ก
การ์ดโชคร้ายทุกใบ **ต้องไม่กระทบผลเกมจริงเด็ดขาด** เป็นแค่ friction ด้าน UX / การรับรู้ / ภาพลวงตา
เท่านั้น — **ยกเว้น 2 ใบ**: ใบ 6 "สับสนกับตัวเอง" (ยังรอ failsafe) และใบ 7 "สติไม่ดี" (เจ้าของโปรเจกต์
ยอมรับความเสี่ยงแล้วโดยตั้งใจ) ส่วนโชคดีเป็นกลไกจริงได้

### พูลโชคดี 10 ใบ
1. **ปากแจ๋ว** — ภูมิคุ้มกัน Silencer 1 ครั้งตลอดเกม
2. **หน้าไหว้หลังหลอก** — เปลี่ยนโหวตได้ 1 ครั้งหลังโหวตไปแล้ว (ก่อนปิดรอบ)
3. **รีเฟล็กซ์วัยรุ่น** — รอดจากหมาป่าเลือกฆ่า 1 ครั้ง/เกม
4. **รอบคอบ** — เวลาโหวตเพิ่ม 10 วิ หลังหมดเวลา
5. **นักสังเกต** — รู้ role หนึ่ง (สุ่ม) มีการใช้สกิลไหม + เป้าหมาย แต่ไม่รู้ว่าใครใช้
6. **ลงมือก่อนที่จะสาย** — ถ้ามี role สกิลกลางคืน → ใช้ได้ตั้งแต่กลางวันเลย
7. **กระซิบข้างหู** — ส่งข้อความลับถึงผู้เล่น 1 คน (คนอื่นไม่เห็น)
8. **ก่อกวน** — ข้อความเด้ง pop-up กลางจอทุกคนเห็น ค้าง 4 วิ พร้อมเสียงเตือน
9. **พยากรณ์** — รู้ล่วงหน้าว่าเช้าพรุ่งนี้เหตุการณ์เป็น "เปลี่ยนกลไก" หรือ "แค่ข้อมูล/บรรยากาศ" (ผูกกับ morning event 9 อันที่มีอยู่ ไม่บอกชื่อจริง)
10. **ให้โอกาส** — เพิ่มเวลาพูดคุยวันนี้อีก 15 วิ (มีผลกับทุกคนในห้อง)

### พูลโชคร้าย 10 ใบ (friction เท่านั้น — ห้ามแตะผลเกม ยกเว้นที่ระบุ)
1. **หูหนวก** — ไม่รับรู้แชทเลย จนเหลือ 15 วิสุดท้ายของช่วงเช้า
2. **ตาพร่ามัว** — รับรู้ผลประกาศ (โหวต/เหตุการณ์) ช้ากว่าคนอื่น
3. **คนนอก** — พิมพ์ข้อความได้แค่ 3 ครั้ง/วัน
4. **นอนน้อย** — เข้าห้องแชทตอนเช้าช้ากว่าคนอื่น
5. **สมองไหล** — มองไม่เห็น countdown ทุก phase 1 รอบ
6. **สับสนกับตัวเอง** ⚠️ — ตอนกดโหวต ต้องเล่นมินิเกมคลิกปุ่มบนจอให้ครบก่อนถึงจะโหวตได้จริง
   **ต้องถาม failsafe ก่อน implement**: ผู้เล่นเล่นมินิเกมไม่ทัน/พลาด จะเกิดอะไร (ปล่อยผ่านโหวตปกติ / ให้ลองใหม่ไม่จำกัด / อื่น ๆ) — **ห้ามลงมือจนกว่าจะได้คำตอบ**
7. **สติไม่ดี** ⚠️ — ตำแหน่ง UI สลับที่ทั้งหมด + เมาส์กลับทิศจริง (ขยับขวา→เคอร์เซอร์ไปซ้าย) เต็มรอบ (เจ้าของยอมรับความเสี่ยงแล้ว ไม่ต้องถามซ้ำ)
8. **เดินไม่ตรงทาง** — ดูแชทเก่าย้อนหลังไม่ได้วันนั้น + ข้อความที่มีอยู่เลือนหายทีละข้อความ
9. **ตื่นตระหนก** — ตอนโหวต ภาพ/เมาส์/ข้อความสั่นไหวปั่นป่วนดูเหมือนโหวตผิดคน แต่ระบบหลังบ้านบันทึกตามตำแหน่งที่กดจริงเท่านั้น
   **สำคัญมาก: แยกชั้นแสดงผล (visual) ออกจาก state การโหวตจริงเด็ดขาด — เอฟเฟกต์ภาพห้ามแตะ logic นับคะแนนแม้แต่จุดเดียว**
10. **ลืมเติมไฟ** — จังหวะสุ่มระหว่างวัน แสงจอสลัวลงวูบ + เสียงกระซิบแผ่ว ๆ (1–2 วิ) แล้วกลับปกติ

### จุดต้องถามก่อนเริ่ม (ระบบ 3)
1. **failsafe ของใบโชคร้าย 6 "สับสนกับตัวเอง"** — เล่นมินิเกมไม่ทันแล้วยังไง (บังคับถามก่อน implement ใบนี้)
2. เก็บ state การ์ด (inventory/ผลที่ active) ไว้ตรงไหน + schema — จะเสนอตอนถึงระบบ 3

---

## ที่ไม่ต้องแตะ (รอบนี้)
- 6 roles เดิม + logic แจก role (ยังไม่เพิ่ม Watchman/Mimic รอบนี้)
- Morning events 9 อัน (ทำงานเหมือนเดิมทั้ง 2 โหมด ไม่แยก logic)
- Signature ลายเส้นวาด, font system, palette 5 สี
- game state machine เดิม (เพิ่ม field ใหม่ได้ ห้ามรื้อของเดิม)

---

## บันทึกการตัดสินใจ (Open Decisions)
| ระบบ | ประเด็น | สถานะ |
|------|---------|-------|
| 1 | เก็บ `gameMode` ที่ไหน | ✅ ตัดสินแล้ว = column `game_mode` (A) · **implemented** |
| 2 | วิธีสะสม vote/death สำหรับ highlights | รอ approve |
| 2 | ข้อความเสียดสีครบทุก highlight | รอเขียน+approve |
| 3 | failsafe ใบ "สับสนกับตัวเอง" (มินิเกมโหวต) | รอ approve (บังคับก่อน implement ใบนี้) |
| 3 | ที่เก็บ state การ์ด (inventory/active) + schema | รอ approve |

> โครงสร้างความน่าจะเป็น/รายการการ์ด 20 ใบ = สเปคยืนยันจากเจ้าของโปรเจกต์แล้ว (ไม่ใช่ข้อเสนอ)
